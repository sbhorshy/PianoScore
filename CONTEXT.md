# PianoScore 术语表

> 本文件记录项目中的领域术语精确定义。不包含实现细节。

## 核心概念

### 手 (Hand)
弹奏钢琴时的物理手。右手和左手。
- 在 MusicXML 中由 `<staff>` 元素标记：`staff=1` = 上谱表（通常右手），`staff=2` = 下谱表（通常左手）
- 当 `<staff>` 标签缺失时，按音高范围回退分配：MIDI ≥ 60 → 右手，< 60 → 左手
- 一首钢琴曲通常同时包含右手和左手的音符

### 练习模式 (Practice Mode)
用户在练习一首曲谱时可选择的模式，决定哪些音符参与评分：

| 模式 | 说明 |
|------|------|
| **右手模式** | 双谱表完整显示，左手音符以灰色参考显示，仅右手的音符参与评分 |
| **左手模式** | 双谱表完整显示，右手音符以灰色参考显示，仅左手的音符参与评分 |
| **双手模式** | 所有音符均参与评分（当前默认行为） |

### 目标音符 (Target Notes)
当前练习模式下需要弹奏并参与评分的音符集合。随练习模式不同而变化。

### 参考音符 (Reference Notes)
当前练习模式下可见但不参与评分的音符。以灰色显示，帮助用户了解另一只手在弹什么。

### 跨手和弦 (Cross-Hand Chord)
同一个 onset 上同时包含左手和右手音符的和弦。例如右手弹 C-E-G 的同时左手弹低八度 C。
在单手练习模式下，用户只需要弹奏属于选中手的音符即可完成该目标。

### 起音 (Onset / Attack)
一次重新敲击琴键的动作。一个目标音符 (Target Notes) 对应一次起音。延续音（被连音线连过来的音）**不是起音**——它在响，但不重新敲击。

### 连音线 (Tie)
连接**两个相同音高**音符的弧线，乐理含义：只弹**一次**，按住到两个音符的**合并时值**。第二个（及后续）音符是延续音，不重新起音。
- 在 MusicXML / OSMD 中由 `Note.NoteTie` 标记（区别于圆滑线）
- 延续音**不产生目标音符**——它不要求用户重新弹奏
- 延续音仍然可见、仍要上色、仍在合并时值内持续发声

### 圆滑线 (Slur)
连接**不同音高**音符的弧线，乐理含义：每个音符**都要弹**，只是演奏得连贯 (legato)。
- 在 OSMD 中由 `Note.NoteSlurs` 标记（与连音线是不同对象）
- 与评分**无关**：圆滑线下的每个音符照常各自起音、各自计分
- 当前不做圆滑线相关的特殊处理

### 部分连音和弦 (Partially-Tied Chord)
同一个 onset 上，部分音是延续音、部分音是新起音的和弦。例如：C 从上一拍连过来 + E、G 新起音。
- 该目标的目标音符**只含需要重新起音的音**（此例为 E、G）
- 延续的 C 不要求重弹，但仍上色、仍在其原起音的合并时值内持续发声

## 架构决策

### 练习模式过滤层
练习模式的数据过滤在 **PracticePage** 层执行：
- PracticePage 根据用户选择的模式，从完整 NoteTarget[] 中筛选出属于选中手的 targets
- 筛选后的 targets 传给 usePractice 进行评分（评分引擎无需知道"手"的概念）
- Staff 组件收到完整的 displayNotes，但只有 filteredTargets 中的音符参与高亮和评分
- 另一只手的音符以灰色参考形式渲染

### NoteTarget 手标记
NoteTarget 通过平行数组 `hands: ('left' | 'right')[]` 标记每个音符的手分配，与 `midiNotes` 一一对应。保持 `midiNotes` 原有结构不变以确保向后兼容。

### 默认练习模式
用户首次打开曲谱时默认为**双手模式**。切换模式后不跨曲谱记忆。

### 手分配回退策略
当 MusicXML 文件不含 `<staff>` 标签时，按音高范围回退分配：MIDI ≥ 60 → 右手，< 60 → 左手。用户仍可在单手/双手模式间切换。

### 模式切换行为
练习过程中切换练习模式会**重置进度**（等同于重新开始练习），因为不同模式的 targets 数量和内容不同。

### 屏幕键盘高亮
VirtualKeyboard 用颜色区分左右手和按键状态：
- **右手**：蓝色系
- **左手**：绿色系
- **错音**：红色
- 单手模式下，非活跃手的按键以淡色显示作为参考（不隐藏）

### 推进模式 (Playback Mode)
用户在练习时，乐谱光标和目标推进的方式。

| 模式 | 说明 |
|------|------|
| **自由练习 (free)** | 现有行为。用户弹对才进入下一个 target。无时间压力，想弹多久弹多久 |
| **跟练模式 (follow)** | 光标按乐谱时间线自动推进（卡拉OK式），速度可调（如 0.5x）。用户必须跟上节拍。弹错或没弹到不影响推进——光标继续走 |

自由练习和跟练模式与练习模式 (Practice Mode) **正交**：可组合为"右手+跟练 0.5x"、"双手+自由练习"等。

跟练模式下光标显示音符时值（长音符亮得久，短音符亮得短），由节拍器/时钟驱动，不依赖 noteOff 事件。

### 部分评分 (Partial Scoring)
跟练模式下，当用户只弹对了和弦中的部分音符但时间已过时，按"命中音数 / 应弹音数"计算，而非简单标为错误。例如和弦 C-E-G 用户只弹了 C-E → 记录为 2/3 命中。

### 听音模式 (Listen Mode)
自动播放乐曲，用户只听不弹。光标按乐谱时间线推进（卡拉OK式）。可与速度设置配合（如 0.5x 慢速播放）。

### 光标 (Cursor)
乐谱上的位置指示器，显示当前演奏/练习位置。视觉为竖线 + 音符颜色高亮。两种推进方式：
- 自由练习：弹对即跳到下一个 target（事件驱动）
- 跟练/听音：按乐谱时间线自动推进（时钟驱动）

### 练习记录模式标记
每次练习 session 记录 `practiceMode: 'right' | 'left' | 'both'` 字段，以便未来按模式筛选历史记录。
