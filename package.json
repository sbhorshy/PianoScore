{
  "name": "pianoscore",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cross-platform piano sight-reading practice app. Frontend in app/, backend in server/, desktop shell in src-tauri/.",
  "scripts": {
    "tauri": "tauri",
    "tauri-dev": ". $HOME/.cargo/env && tauri dev",
    "tauri-build": ". $HOME/.cargo/env && tauri build",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@tauri-apps/cli": "^1.5.0"
  }
}
